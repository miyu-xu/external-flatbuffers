# FlatBuffers（Android 固定版本）

[English](readme.md) | 简体中文

本仓库提供 BSCP 构建使用的 FlatBuffers 固定 Android 版本。它是序列化支撑依赖，不承载
BSCP 运行时策略。版本由 manifest 固定；升级必须同时验证 aemu、gfxstream、crosvm 及
Android 构建兼容性，不得提交生成目录。

上游 API、构建与语言使用说明以英文 README 和上游文档为准。
